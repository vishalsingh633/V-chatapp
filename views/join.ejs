<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join Room</title>
  <style>
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #c2e9fb, #a1c4fd);
  padding: 0;
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}

.form-container {
  background: rgb(155, 67, 67);
  width: 90%;
  max-width: 400px;
  
  padding: 2rem;
  border-radius: 16px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  text-align: center;
  animation: slideUp 0.4s ease-out;
}

@keyframes slideUp {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

h2 {
  color: #333;
  font-size: 1.75rem;
  margin-bottom: 1.5rem;
}

input {
  width: 100%;
  padding: 0.9rem;
  margin-bottom: 1rem;
  border: 1px solid #ccc;
  border-radius: 10px;
  font-size: 1rem;
  background: #f9f9f9;
  transition: all 0.3s ease;
}

input:focus {
  border-color: #007bff;
  background: #fff;
  box-shadow: 0 0 5px rgba(0, 123, 255, 0.2);
  outline: none;
}

button {
  width: 100%;
  padding: 0.9rem;
  background-color: #007bff;
  color: white;
  font-size: 1rem;
  font-weight: 600;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  transition: 0.3s ease;
}

button:hover {
  background-color: #0056b3;
  transform: scale(1.02);
}

.error {
  background-color: #ffd2d2;
  color: #b10000;
  padding: 0.6rem;
  border-radius: 6px;
  margin-bottom: 1rem;
  font-size: 0.95rem;
}

.back-link {
  display: inline-block;
  margin-top: 1.2rem;
  color: #007bff;
  text-decoration: none;
  font-weight: bold;
  font-size: 1rem;
}

.back-link:hover {
  text-decoration: underline;
}

/* 🌐 Mobile-first: Let the form grow more on smaller screens */
@media (max-width: 480px) {
  .form-container {
    width: 95%;
    padding: 1.5rem 1.2rem;
    border-radius: 12px;
  }

  h2 {
    font-size: 1.4rem;
  }

  input,
  button {
    font-size: 0.95rem;
    padding: 0.75rem;
  }

  .back-link {
    font-size: 0.95rem;
  }
}

  </style>
</head>
<body>
  <div class="form-container">
    <h2>Join a Chat Room</h2>

    <% if (typeof error !== 'undefined') { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form action="/room/join" method="POST">
      <input type="text" name="name" placeholder="Room Name" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Join Room</button>
    </form>

    <a href="/room/dashboard" class="back-link">← Back to Dashboard</a>
  </div>
</body>
</html>

