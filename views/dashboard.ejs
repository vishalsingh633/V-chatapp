<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login Success</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background-color: grey;
      position: relative;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      color: #fff;
    }

    .overlay {
      position: absolute;
      top: 0; 
      left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0, 0, 0, 0.6);
      z-index: 0;
    }

    .container {
      position: relative;
      z-index: 1;
      padding: 40px 20px;
      max-width: 800px;
      width: 100%;
      background-color: rgba(255, 255, 255, 0.95);
      margin: 60px auto;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
      color: #333;
    }

    h1, h2 {
      text-align: center;
      margin-bottom: 10px;
    }

    p {
      text-align: center;
      margin-bottom: 30px;
    }

    nav {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 15px;
      margin-bottom: 30px;
    }

    nav a {
      text-decoration: none;
      background-color: #4facfe;
      padding: 10px 20px;
      border-radius: 8px;
      color: white;
      font-weight: bold;
      transition: 0.3s;
    }

    nav a:hover {
      background-color: #3a8bd4;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      background-color: #f1f1f1;
      margin: 10px 0;
      padding: 12px 20px;
      border-radius: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    li a {
      color: #4facfe;
      font-weight: bold;
      text-decoration: none;
    }

    li a:hover {
      text-decoration: underline;
    }

    @media (max-width: 600px) {
      .container {
      width: 300px;
      }

      nav {
        flex-direction: column;
        gap: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="overlay"></div>
  <div class="container">
    <h1>Login Successful!</h1>
    <h2>Welcome, <%= currentUser.username %>.</h2>
    <p>You are now logged in. What would you like to do?</p>

    <nav>
      <a href="/room/host">Host Room</a>
      <a href="/room/join">Join Room</a>
      <a href="/logout">Logout</a>
    </nav>

    <h3>Available Rooms</h3>
    <ul>
      <% rooms.forEach(room => { %>
        <li>
          <span><%= room.name %></span>
          <a href="/room/join/<%= room._id %>">Join</a>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>

